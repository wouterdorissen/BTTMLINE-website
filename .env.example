# Kopieer dit bestand naar .env (lokaal) en zet dezelfde waarde in Vercel als
# een Environment Variable. .env staat in .gitignore en komt dus niet in de repo.

# Zapier catch-hook die de webinar-inschrijvingen ontvangt (/inzichten).
PUBLIC_WEBINAR_WEBHOOK=
